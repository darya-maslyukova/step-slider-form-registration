export function  paginationProgress (currentStep) {
    const slider = document.getElementById('step-slider');
    const pagination = slider.querySelector('.step-slider__pagination')

    const countSteps = 5;

    console.log(currentStep);
}

